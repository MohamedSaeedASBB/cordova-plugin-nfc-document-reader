// Stub of MLKitVision — only what this plugin uses.
import Foundation
import UIKit
import CoreMedia

public class VisionImage {
    public var orientation: UIImage.Orientation = .up
    public init(image: UIImage) {}
    public init(buffer sampleBuffer: CMSampleBuffer) {}
}
